# Explaining inconsistent event scheduling instance

`art_event.lp` is an inconsistent event scheduling instance.

`eventschedule.lp` is the main encoding file.

`explain.py` is an example usage of the xpit library. Run it as shown below:

```
python explain.py
```
